'use client'
import { useState } from 'react'
import { Bot, Check, Copy, Globe2, Power, Save } from 'lucide-react'
const code = '<script src="https://saad1901.github.io/vbase/widget.js" data-tenant="tn_8Y2A"></script>'
export default function WidgetsPage() { const [enabled,setEnabled]=useState(true),[greeting,setGreeting]=useState('Hi! How can I help?'),[copied,setCopied]=useState(false); const copy=async()=>{await navigator.clipboard.writeText(code);setCopied(true);setTimeout(()=>setCopied(false),1600)}; return <div className="space-y-6"><header className="flex flex-col gap-4 border-b border-border pb-6 sm:flex-row sm:items-end sm:justify-between"><div><p className="font-mono text-xs uppercase tracking-[0.2em] text-primary">Assistant delivery</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Widget control</h1><p className="mt-2 text-sm text-muted-foreground">Manage deployed chat widgets, delivery safety, and global defaults.</p></div><button className="inline-flex items-center justify-center gap-2 rounded-lg bg-primary px-3.5 py-2 text-xs font-semibold text-primary-foreground"><Save className="size-3.5"/>Save changes</button></header><section className="grid gap-5 lg:grid-cols-[1.1fr_1fr]"><div className="rounded-2xl border border-border bg-card p-5 shadow-xs sm:p-6"><div className="flex justify-between"><div><h2 className="font-semibold">Global delivery switch</h2><p className="mt-1 text-xs text-muted-foreground">Pausing stops widgets on all customer domains.</p></div><button onClick={()=>setEnabled(!enabled)} className={`relative h-7 w-12 rounded-full ${enabled?'bg-emerald-500':'bg-muted-foreground'}`} aria-label="Toggle widget delivery"><span className={`absolute top-1 size-5 rounded-full bg-white shadow ${enabled?'left-6':'left-1'}`}/></button></div><div className="mt-6 grid grid-cols-3 gap-3 border-y border-border py-5 text-center"><Stat value="232" label="Live"/><Stat value="11" label="Draft"/><Stat value="5" label="Paused"/></div><div className="mt-5 flex items-center gap-2 text-xs"><Power className={`size-4 ${enabled?'text-emerald-500':'text-rose-500'}`}/><span className="font-medium">Delivery is {enabled?'enabled':'paused'}</span></div></div><div className="rounded-2xl border border-border bg-card p-5 shadow-xs sm:p-6"><div className="flex items-center gap-2"><Bot className="size-5 text-primary"/><h2 className="font-semibold">Default assistant settings</h2></div><label className="mt-5 block text-xs font-medium">Greeting message<textarea value={greeting} onChange={e=>setGreeting(e.target.value)} className="mt-2 min-h-24 w-full rounded-lg border border-input bg-background p-3 text-sm outline-none focus:ring-2 focus:ring-ring"/></label><label className="mt-4 block text-xs font-medium">Default position<select className="mt-2 h-10 w-full rounded-lg border border-input bg-background px-3 text-sm"><option>Bottom right</option><option>Bottom left</option></select></label></div></section><section className="rounded-2xl border border-border bg-card p-5 shadow-xs sm:p-6"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-semibold">Embed snippet</h2><p className="mt-1 text-xs text-muted-foreground">Default script for a tenant’s website. Tenant keys must be generated per workspace.</p></div><button onClick={copy} className="inline-flex items-center gap-2 rounded-lg border border-border px-3 py-2 text-xs font-semibold hover:bg-muted">{copied?<Check className="size-3.5 text-emerald-600"/>:<Copy className="size-3.5"/>}{copied?'Copied':'Copy snippet'}</button></div><pre className="mt-5 overflow-x-auto rounded-xl bg-muted p-4 text-xs text-foreground"><code>{code}</code></pre></section><section className="rounded-2xl border border-border bg-card p-5 shadow-xs"><div className="flex items-center gap-2"><Globe2 className="size-5 text-primary"/><div><h2 className="font-semibold">Domain verification queue</h2><p className="text-xs text-muted-foreground">3 tenant domains are awaiting verification.</p></div></div><div className="mt-5 grid gap-3 sm:grid-cols-3"><Domain name="support.atlasfinance.com" state="Pending DNS"/><Domain name="help.beryl.io" state="Pending DNS"/><Domain name="app.cedar.co" state="Review required"/></div></section></div> }
function Stat({value,label}:{value:string;label:string}) {return <div><p className="text-lg font-bold">{value}</p><p className="text-[10px] text-muted-foreground">{label}</p></div>}
function Domain({name,state}:{name:string;state:string}) {return <div className="rounded-xl border border-border p-3"><p className="truncate text-xs font-medium">{name}</p><p className="mt-1 text-[11px] text-amber-600">{state}</p></div>}
